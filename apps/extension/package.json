{
  "name": "@signets/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint --cache --cache-location .cache/eslint \"src/**/*.ts\" \"test/**/*.ts\" \"scripts/**/*.mjs\"",
    "lint:fix": "eslint --cache --cache-location .cache/eslint --fix \"src/**/*.ts\" \"test/**/*.ts\" \"scripts/**/*.mjs\"",
    "format": "prettier --write .",
    "test": "vitest run",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "@signets/shared": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.313",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3",
    "vitest": "^3.0.9"
  }
}
