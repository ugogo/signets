# AGENTS.md

## Agent skills

### Issue tracker

Work is tracked as checkbox lists inside plan documents (`.scratch/<feature-slug>/plan.md`) — no external issue tracker. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
